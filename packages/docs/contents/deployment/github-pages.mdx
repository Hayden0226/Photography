---
title: GitHub Pages
description: Guide to deploying Jacky's Photography via GitHub Pages.
createdAt: 2025-07-20T22:35:03+08:00
lastModified: 2026-05-25T00:00:00+01:00
---

# GitHub Pages Deployment

The current deployment workflow publishes the static gallery build from `apps/web/dist/` to GitHub Pages.

## Repository Settings

In GitHub, configure:

- **Pages source**: GitHub Actions.
- **Workflow**: `.github/workflows/deploy.yml`.
- **Branch**: `main`.

The workflow declares the `github-pages` environment and deploys through `actions/deploy-pages`.

## Local Build Equivalent

To reproduce the main build locally:

```bash
pnpm install
git clone git@github.com:Jackyhq/Photography-Photos.git photos
pnpm run photos:standardize
pnpm run build:manifest
pnpm build
```

The deployable static files will be in:

```plain
apps/web/dist/
```

## Workflow Output

During CI, the workflow uploads:

```plain
apps/web/dist/
```

The same directory is mirrored into the external deployment repository `Jackyhq/Photography-Web`. The root `web/` directory is ignored in this source repository and should not be treated as the canonical build output.

Photo source files stay in the private `Jackyhq/Photography-Photos` repository. The generated manifest lives at `apps/web/src/data/photos-manifest.json` during local builds and CI; `packages/data/src/photos-manifest.json` is a tracked symlink to that generated file.

## Custom Domain

The gallery site configuration currently points to:

```plain
https://photo.jackyw.cn
```

Keep `config.json` and your GitHub Pages custom-domain/DNS settings aligned so canonical URLs, OpenGraph metadata, and sitemap output use the deployed domain.
